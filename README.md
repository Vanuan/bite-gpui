# 🎬 bite-gpui 🍟

`bite-gpui` is a high-performance, modular fork of the **GPUI** rendering ecosystem. 
Unlike rigid UI frameworks, `bite-gpui` is designed like a fast-food kiosk:
you choose your layout, text system, pipeline, and rendering engine layer by layer.
